spring:
  jpa:
    hibernate:
      ddl-auto: none
  security:
    jwt:
      secret-key: test-jwt-secret-key
      expiration: 900000
    refresh-token:
      expiration: 604800000
    oauth2:
      client:
        registration:
          google:
            client-id: test-id
            client-secret: test-secret
app:
  frontend:
    url: http://localhost:3000